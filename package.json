{
  "name": "static-store",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "store:build": "bash build-store.sh",
    "store:aggregate": "bash build-store.sh --aggregate",
    "store:validate": "bash build-store.sh --dry-run",
    "publish": "bash make-publish.sh",
    "publish:aggregate": "bash make-publish.sh --aggregate",
    "publish:deploy": "bash make-publish.sh --deploy-only",
    "publish:dry-run": "bash make-publish.sh --dry-run"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
